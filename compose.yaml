services:
  dynamodb-local:
    image: amazon/dynamodb-local
    container_name: dynamodb-local
    ports:
      - "8000:8000"
    networks:
      - sam-local
    command: -jar DynamoDBLocal.jar -sharedDb

networks:
  sam-local:
    name: sam-local
