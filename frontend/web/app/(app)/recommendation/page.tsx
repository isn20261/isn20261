/**
 * Phase 6 (HOME-03 stub target, issue #95) — /recommendation placeholder.
 * Phase 7 (RECO-01..04) replaces this in place with the real result screen.
 *
 * Public route — does not live inside (protected). The recommendation flow
 * is intentionally usable by signed-out visitors.
 */

export default function RecommendationPage() {
  return (
    <div className="p-10">
      <h1 className="font-display text-28 font-semibold text-text-primary">
        Recommendation
      </h1>
      <p className="text-text-secondary text-14 mt-2">
        Phase 7 (RECO-01..04) will fill this page.
      </p>
    </div>
  );
}
