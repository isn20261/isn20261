# Phase 15 · Plan 15-02 — Verification gate + phase close

**Authored:** 2026-05-14
**Plan number:** 2 of 2 (P15-01 → **P15-02**)
**Branch:** `feature/issue-134-history-integration`
**Depends on:** Plan 15-01 committed.
**Gates:** confirm_plan (this doc); on completion → open PR + transition.
**Estimated effort:** ~15 minutes (automated gates) + smoke deferred

---

## Goal

Verify Phase 15 against the issue #134 acceptance criteria and the ROADMAP §Phase 15 success criteria. Produce per-plan SUMMARYs and the phase-close artifact, mirroring Phase 14's 14-03 structure.

---

## File changes

| File | Change |
|---|---|
| `.planning/phases/15-history-integration/15-01-SUMMARY.md` | NEW |
| `.planning/phases/15-history-integration/15-02-SUMMARY.md` | NEW (phase-close) |

No production code change.

---

## Block A — Static gates (declared in 15-01-PLAN, verified here)

```bash
cd frontend/web

# A1. TypeScript strict
pnpm exec tsc --noEmit
# Expected: exit 0.

# A2. ESLint
pnpm lint
# Expected: exit 0.

# A3. Next.js production build
pnpm build
# Expected: exit 0; 14/14 pages prerendered.

# A4. No mock fallback
git grep -n 'mock' lib/api/history.ts
# Expected: 0 hits.

# A5. No raw fetch outside wrapper
git grep -nE '\bfetch\(' lib/api/history.ts 'app/(app)/(protected)/history/'
# Expected: 0 hits.

# A6. DSGN-06: no hex literals introduced
git grep -nE 'className=.*#[0-9a-fA-F]{3,6}' 'app/' 'components/' | grep -v 'app/tokens/page.tsx'
# Expected: 0 hits (Phase 15 doesn't introduce any).

# A6b. DSGN-06: no new inline style props introduced
git diff --stat backend-integration..HEAD -- 'app/' 'components/' | grep -E '\.tsx?$'
# Inspect — only history/page.tsx + smoke/page.tsx expected.
```

## Block B — UI compliance (manual)

```bash
# B1. Section order: Today → Yesterday → Last week → Earlier (when entries exist)
# B2. Empty state matches Phase 9 EmptyState component (reused as-is)
# B3. Three breakpoints: page-level layout unchanged from Phase 9 (max-w-[920px] column)
```

## Block C — Live-AWS smoke (SKIPPED-AWS-DEFERRED, run-when-home checklist)

```
[ ] cd frontend/web; pnpm dev
[ ] NEXT_PUBLIC_API_BASE_URL=<pulumi stack output api_internal_url>
[ ] Sign in as a teammate account that has some recommendation history
[ ] Visit /history

— Network panel —
[ ] GET /api/v1/history fires on mount; status 200
[ ] Response body matches: [{ title, "recommended-at" }, ...]
[ ] Response is sorted newest-first

— Rendering —
[ ] Rows appear in order; titles match the Lambda response
[ ] Relative-time labels render reasonably (Today / Yesterday / weekdays / dates)
[ ] Bucket headers (Today / Yesterday / Last week / Earlier) appear only when their bucket has entries

— Empty path —
[ ] Use a freshly-signed-up account with no history
[ ] GET /api/v1/history returns 200 with body []
[ ] <EmptyState /> renders with "Pick a movie for me" CTA pointing to /

— Error UX —
[ ] DevTools → Network → Offline; reload /history
[ ] useApiErrorUx fires the network-kind toast
[ ] Skeleton resolves to no rows visible; no crash

— Unauthorized —
[ ] Force-expire / invalidate the Cognito IdToken in DevTools Application
[ ] Reload /history
[ ] Wrapper detects 401 → onUnauthorized → signOut → RequireAuth redirects to /login
```

## Block D — Acceptance criteria mapping (issue #134)

| AC | Coverage |
|---|---|
| Visitar /history autenticado dispara `GET /history` real; lista reflete a resposta do Lambda | ✅ Code path; ⏳ smoke |
| Conta nova sem histórico → empty state per o design do Phase 9 | ✅ Code path (`<EmptyState />` reused); ⏳ smoke |
| Devtools offline → error UX do wrapper, não lista em branco | ✅ Code path (`useApiErrorUx` toast); ⏳ smoke |
| `git grep -n 'mock' frontend/web/lib/api/history*` → 0 hits | ✅ Block A4 |

## Deferred items

1. Live-AWS smoke — run-when-home.
2. **v2.1 backend enrichment**: `/history` response shape (add poster URL / mood / genre / runtime / id).
3. **v2.1 frontend**: re-instate rich rows after backend enrichment lands.
4. **v2.1 pagination**: if history payloads exceed a few hundred items.
5. **Phase 17 cleanup**: delete orphaned `<HistoryRow />` component.

---

## Commit message

```
docs(15): Phase 15 verification gate + closure summaries (15-02)

Phase 15 plan 15-02 — Block A static gates verified; per-plan SUMMARYs
authored; phase-close artifact written. Block C live-AWS smoke
SKIPPED-AWS-DEFERRED with run-when-home checklist preserved mirroring
Phase 12 / Phase 14 patterns.

- 15-01-SUMMARY.md: lib + page rewrite closure. Documents any deviations.
- 15-02-SUMMARY.md: phase-close. Run-when-home smoke checklist + AC map.

Phase 15 closed; STATE.md / ROADMAP.md transition next, then PR open.

Refs #134, #127
```

---

## Rollback

Documentation-only.

---

*Plan: 15-02-PLAN.md*
*Awaits user approval per gates.confirm_plan.*
